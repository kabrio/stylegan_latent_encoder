entrypoint: python runway_train_model.py
python: 3.6
framework: tensorflow
cuda: 9.0
spec:
  gpu: True
  cpu: False
files:
  ignore:
    - checkpoints/*
build_steps:
  - pip install matplotlib
  - pip install Pillow
  - pip install tqdm
  - pip install -r requirements.txt